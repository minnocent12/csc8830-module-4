<!-- Claude Code adapter for Module 4. Canonical instructions live in AGENTS.md files
     (shared with Codex). The parent imports below load the shared root rules and Git
     protocol when the parent directory is reachable; if this module is opened as a
     standalone repository without the parent, those imports are skipped and the root
     rules must be supplied another way (see Module_4/AGENTS.md). -->

@AGENTS.md
@../AGENTS.md
@../GIT_WORKFLOW.md

# Module 4 results

The result directories remain empty because no RGB or thermal academic experiment has been run.
Phase 4's runner can write derived JSON/CSV records to new paths when the user supplies explicit
inputs and references. Missing references are recorded as pending with null metrics rather than
as zero scores. No SAM2 mask or empirical metric is bundled here.
